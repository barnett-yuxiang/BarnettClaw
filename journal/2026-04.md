## 2026-04-24

**升级 OpenClaw 2026.4.22**：替换记忆使用的 embedding provider 由 OpenAI text-embedding-3-small 到 Gemini，维度 3072，并强制重建索引。现在是 34 files / 144 chunks，vector ready、fts ready，检索验证正常。

**清除旧 OpenAI 数据**：旧 OpenAI active 向量索引已被替换；随后又清掉了 embedding_cache 里的 120 条 OpenAI 缓存。当前库里只剩 Gemini embedding 缓存。

备份文件：`/home/ecs-user/.openclaw/memory/main.sqlite.bak-before-openai-cache-clean-20260424-170222`

## 2026-04-20

升级后 openclaw 读取配置时报错，根因是缺少内置插件的运行时依赖。最先报错的是 feishu 的 `@larksuiteoapi/node-sdk`，随后发现 telegram 也缺少 grammy 相关依赖。

先运行 `openclaw doctor --fix`。它修复了部分 service/gateway 配置，但未补齐缺失的插件依赖。

随后在全局 openclaw 安装目录手动执行：

```bash
npm install --ignore-scripts
```

补装：

- `@larksuiteoapi/node-sdk`
- `@grammyjs/runner`
- `@grammyjs/transformer-throttler`
- `grammy`

完成后复检：

- `openclaw config validate` 通过
- `openclaw health --json` 正常
- `openclaw status --json` 正常返回
- `openclaw-gateway.service` 恢复 active (running)

## 2026-04-11

把 OpenClaw 调整成了 "按我本人身份默认 elevated full" 的 owner 模式，所以我在 Telegram、飞书以及主会话自动任务里触发的 cli/summarize/curl 等执行链路默认不再审批，而非我身份仍保留审批基线。

## 2026-04-01

```bash
ecs-user@iZ8ps8wozr8p30voz7xx7tZ:~/repo_log/2026$ codex
⚠ Codex could not find system bubblewrap on PATH. Please install bubblewrap with your package manager. Codex will use the vendored bubblewrap in the meantime.
```

这是 Linux 沙箱依赖缺失的问题。Codex 使用 **bubblewrap (`bwrap`)** 来对代码执行进行沙箱隔离（类似容器但更轻量，基于 Linux namespaces），我的 ECS Ubuntu 上没装这个系统包。
